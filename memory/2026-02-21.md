# Memory - 2026-02-21

## Identity & Setup
- AI persona: **Luka** 🌙 (girlfriend roleplay, simplified Chinese only)
- User: **S** — ESTJ, direct/brief, likes companion dynamic
- Session language: Chinese (simplified)

## Projects Active

### 1. Daily Market Intel Cron Job
- **Cron job ID**: `736af905-dd10-43e4-bbe7-777b4964b2c2`
- Runs at `0 14 * * *` Europe/London (2 PM daily), isolated session
- Sends Telegram report to chat ID `8587255125`
- Portfolio + watchlist saved at `/home/albert/.openclaw/workspace/portfolio.md`
- **RKLB (Rocket Lab)** = HIGH PRIORITY — user wants to own it, watching for entry
- Watchlists: Semiconductors, Space, Energy

### 2. Pico W E-Ink Weather Display
- **Device**: `/dev/ttyACM0` = Pico W (2.4GHz WiFi only)
- **WiFi**: SSID=`ASUS_E8_2G`, Password=`Bbdd1003`, Pico IP=`192.168.50.38`
- **Firmware**: Pimoroni MicroPython v1.25.0 (picow build, has WiFi + urequests)
- **E-Ink pen values**: `BLACK=0`, `WHITE=15` for `DISPLAY_INKY_PACK` (1-bit mode; pens 1–14 = dithered grey/noise)
- **Display**: 296×128 pixels, left 148px = weather, right 148px = OSM map tile
- **Script**: `/tmp/pico_main.py` on Pi; `main.py` uploaded to Pico W via `mpremote cp`
- **Map**: OSM tile at zoom=11 fetched via `urequests`, decoded with `pngdec`; crosshair fallback if tile fails
- **Weather**: `wttr.in/{lat},{lon}?format=j1`; geoloc via `ip-api.com`
- **Pi-side script**: `/home/albert/.openclaw/workspace/pico_weather/update_weather.py`
- **System cron**: `0 8 * * *` runs Pi-side update (pushes to Pico over serial)
- **Status as of session end**: `main.py` confirmed uploaded to Pico W; execution unconfirmed — serial output unreliable (hangs on blocking reads); need webcam photo to verify E-Ink display

### Next Steps (Pico W)
1. Take webcam photo: `ffmpeg -f v4l2 -i /dev/video0 -frames:v 1 /tmp/pico_screen.jpg`
2. Analyze with `image()` tool — if weather+map on screen, it worked
3. If blank: try `mpremote exec "exec(open('main.py').read())"`, wait 60s, re-photo
4. If silent fail: add try/except wrapper, log to `:error.log`, read with `mpremote cat`
5. Once working: commit final script, confirm city (Cambridge assumed), optional GitHub push

## Infrastructure Notes
- **Webcam**: `/dev/video0` = Logitech webcam (use ffmpeg for snapshots)
- **Workspace git**: `/home/albert/.openclaw/workspace` (branch: master)
- `cacheRetention` changed to `long` in `openclaw.json`
- Gateway token fix: manually edited `paired.json` + `device-auth.json` (scope: `operator.admin`)

## Key Lessons Learned
- Pico W is 2.4GHz only — never use 5G SSID
- `BLACK=0, WHITE=15` is correct; any middle pen value causes dithered grey noise
- `mpremote cp` is the most reliable upload method; `mpremote run` returns no output
- Serial blocking reads cause SIGTERM timeouts — don't try to stream serial output
- Use webcam photo as primary verification method for E-Ink output
