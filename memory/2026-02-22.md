# Daily Notes — 2026-02-22

## Pico W E-Ink Weather Display — Status at Session End

### What's Done
- `pico_main.py` (22,952 bytes) is the final version: self-contained, embeds UK map as bytes (`UK_MAP`), uses `jpegdec.open_RAM(UK_MAP)`, city lookup table (`CITY_DOTS`), `parse_time()` from open-meteo API, WiFi + geolocation + weather + e-ink
- File is in workspace at `/home/albert/.openclaw/workspace/pico_weather/pico_main.py` and `/tmp/pico_main.py`
- `uk_map.jpg` no longer needed on Pico flash — embedded in main.py
- Git committed: `feat: embed UK map as bytes in main.py + city lookup table — zero external files`

### Blocked / Pending
- **mpremote is broken**: exits silently with code 0, no output; needs `pip3 install --force-reinstall mpremote`
- **Pico W USB state unknown**: may be stuck in display.update() or crash loop; S asked to physically replug USB
- **Display not yet verified** with webcam (room was dark)

### Next Steps (resume here)
1. Fix mpremote: `pip3 install --force-reinstall mpremote --break-system-packages`; verify with `python3 -m mpremote --help`
2. Confirm S has replugged Pico W USB
3. Upload: `mpremote connect /dev/ttyACM0 cp /tmp/pico_main.py :main.py + reset`
4. Wait ~50-90s for display to update
5. Take webcam photo to verify output (date/time, location dot, weather)
6. Commit final verified version

### Key Constants
- WiFi: `ASUS_E8_2G` / `Bbdd1003`, Pico IP: `192.168.50.38`
- Pen values: `BLACK=0`, `WHITE=15` (never use 1-14)
- Device: `/dev/ttyACM0` (Pico W), `/dev/video0` (webcam)
- Map bounds: `LON_MIN=-6.5, LON_MAX=2.1, LAT_MIN=49.8, LAT_MAX=60.9, PAD=6`
- Map display coords: `MAP_X=148, MAP_Y=14, MAP_W=148, MAP_H=113`

## Market Intel Cron
- Job ID: `736af905-dd10-43e4-bbe7-777b4964b2c2`
- Runs 2 PM Europe/London daily → Telegram `8587255125`
- RKLB (Rocket Lab) = HIGH PRIORITY watchlist item
- Portfolio file: `/home/albert/.openclaw/workspace/portfolio.md`
